# archive/

One-off and superseded scripts, kept for reference only. Not maintained, not
guaranteed to run against the current layout (paths/imports may be stale).

Superseded by the maintained scripts under `scripts/`:
- `plot_feature_drift.py`, `plot_feature_drift_original_space.py` → `scripts/viz/plot_density_drift*.py`
- `plot_tsne_temporal.py`, `plot_week1_temporal.py` → `scripts/viz/plot_tsne_temporal_static.py`, `plot_pca_temporal.py`

The rest are single-use plots / fixes / scratch tests (`plot_paper_figures.py`,
`plot_bbse_per_class.py`, `plot_error_histograms.py`, `plot_macro_f1.py`,
`plot_cm_diffs.py`, `plot_label_shift.py`, `fix_npz_embeddings.py`,
`test_sld_em*.py`, `test_inference_sanity.py`).

Safe to delete once you're sure nothing here is still needed.
